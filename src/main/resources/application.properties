spring.application.name=gestao_vagas
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/gestao_vagas}
spring.datasource.username=${DATABASE_USERNAME:postgres}
spring.datasource.password=${DATABASE_PASSWORD:postgres}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

security.token.secret="mach0vinh@ssuavedemaiscara2025_123!"
security.token.secret.candidate="mach0vinh@ssuavedemaiscara2025_432!"

management.endpoints.web.exposure.include=health,metrics,prometheus,loggers,threaddump
management.endpoints.access.default=none
management.endpoint.health.access=read-only
management.endpoint.metrics.access=read-only
management.endpoint.prometheus.access=read-only
management.endpoint.loggers.access=read-only
management.endpoint.threaddump.access=read-only

management.endpoint.health.show-details=always
management.prometheus.metrics.export.enabled=true


